# Add these values in Vercel → Project → Settings → Environment Variables
TELEGRAM_BOT_TOKEN=
TELEGRAM_ORDER_CHAT_ID=

# Supabase Project Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# OTCommerce / Poizon China — server only, never expose as NEXT_PUBLIC_*
OTAPI_INSTANCE_KEY=

# INNER retail pricing — server only
# Example meaning: 1 CNY = X RUB, fixed delivery per item in RUB, margin percentage.
INNER_CNY_TO_RUB=
INNER_DELIVERY_RUB=
INNER_MARGIN_PERCENT=
